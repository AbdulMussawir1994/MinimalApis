namespace MinimalApis.Enums;

public enum ExpenseCategory
{
    Home,
    Personal,
    Family,
    Other
}

public enum ExpenseType
{
    Prepaid, //Payment: Pay in advance for a set amount of data, calls, and texts.
    Postpaid// Payment: Receive a monthly bill for services used during the cycle (pay after).
}
